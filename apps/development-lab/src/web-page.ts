export function renderDevelopmentPage(): string {
  return `<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="description" content="ProductWoc 本地开发执行工作台" />
  <title>ProductWoc · Development</title>
  <style>
    :root{color-scheme:light;--ink:#17201c;--muted:#68736d;--line:#d9e1dc;--paper:#f2f5f1;--card:#fff;--green:#176347;--green2:#d9f0df;--amber:#fff1cf;--red:#9d382f;--blue:#e7efff;--shadow:0 18px 50px #17342612}
    *{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;background:var(--paper);color:var(--ink);font:15px/1.55 Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}button,select{font:inherit}button:focus-visible,select:focus-visible,a:focus-visible,summary:focus-visible{outline:3px solid #6b9fff;outline-offset:3px}.skip{position:fixed;left:16px;top:-80px;z-index:10;background:#fff;padding:10px 14px;border-radius:10px}.skip:focus{top:16px}.shell{min-height:100vh;display:grid;grid-template-columns:270px minmax(0,1fr)}.rail{min-width:0;background:#10271e;color:#eef8f1;padding:26px 22px;display:flex;flex-direction:column;gap:28px}.brand{font-weight:800;font-size:19px}.brand span{display:inline-grid;place-items:center;width:34px;height:34px;margin-right:10px;border-radius:10px;background:#dffc9b;color:#173426}.rail small{color:#acc0b4}.rail nav{display:grid;gap:8px}.rail a{color:#d9e7df;text-decoration:none;padding:9px 11px;border-radius:10px}.rail a:hover{background:#ffffff12}.local{margin-top:auto;border-top:1px solid #ffffff22;padding-top:18px;color:#b7c8bf;font-size:12px}main{min-width:0;padding:34px clamp(20px,4vw,58px) 64px}.top{display:flex;justify-content:space-between;align-items:flex-start;gap:20px}.eyebrow{text-transform:uppercase;letter-spacing:.12em;font-size:11px;font-weight:800;color:var(--green)}h1{font-size:clamp(32px,5vw,56px);line-height:1.03;letter-spacing:-.045em;margin:8px 0 12px;max-width:900px}h2,h3,p{margin-top:0}.lede{font-size:17px;color:var(--muted);max-width:760px}.status{border-radius:999px;padding:8px 12px;background:var(--green2);font-weight:750;white-space:nowrap}.meta,.actions{display:flex;gap:8px;flex-wrap:wrap}.pill{border:1px solid var(--line);border-radius:999px;padding:6px 10px;background:#fff;font-size:12px}.grid{display:grid;grid-template-columns:minmax(0,1.55fr) minmax(310px,.75fr);gap:18px;margin-top:24px}.stack{display:grid;gap:18px}.card{background:var(--card);border:1px solid var(--line);border-radius:18px;box-shadow:var(--shadow);overflow:hidden}.card-head{display:flex;justify-content:space-between;align-items:center;gap:14px;padding:17px 19px;border-bottom:1px solid var(--line)}.card-head h2,.card-head h3{margin:0}.card-body{padding:19px}.phase{border-left:4px solid #bbc7bf}.phase.running,.phase.ready{border-left-color:var(--green)}.phase.completed{border-left-color:#71a589}.tasks{display:grid;gap:9px}.task{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:10px;padding:12px;border:1px solid var(--line);border-radius:12px}.task strong,.task small{display:block}.task small,.muted{color:var(--muted)}.current{background:#f7fff8;border-color:#9fc9ad}.trace{font:12px/1.55 ui-monospace,SFMono-Regular,Menlo,monospace;color:#526159;overflow-wrap:anywhere}.blocker{padding:13px 15px;border-radius:13px;background:var(--amber);color:#634817;margin-bottom:10px}.blocker.stale,.blocker.workspace_drift{background:#fff0ef;color:var(--red)}.summary-grid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px}.metric{padding:13px;border:1px solid var(--line);border-radius:13px}.metric strong{display:block;font-size:24px}.primary,.secondary,.danger{border-radius:11px;padding:10px 13px;font-weight:750;cursor:pointer}.primary{border:0;background:var(--green);color:#fff}.secondary{border:1px solid var(--line);background:#fff;color:var(--ink)}.danger{border:1px solid #edc3be;background:#fff8f7;color:var(--red)}button:disabled{opacity:.45;cursor:not-allowed}details{border-top:1px solid var(--line)}details:first-child{border-top:0}summary{cursor:pointer;padding:14px 18px;font-weight:750}.detail{min-width:0;padding:0 18px 18px}.file{display:grid;grid-template-columns:90px minmax(0,1fr);gap:10px;padding:8px 0;border-bottom:1px dashed var(--line)}.file>*{min-width:0}pre{margin:0;max-height:360px;overflow:auto;white-space:pre-wrap;overflow-wrap:anywhere;background:#12231c;color:#dcebe2;padding:15px;border-radius:12px;font:12px/1.6 ui-monospace,SFMono-Regular,Menlo,monospace}.sticky{position:sticky;top:20px}.model-select{min-width:0;max-width:100%;width:100%;padding:10px;border:1px solid var(--line);border-radius:10px;background:#fff}.busy{opacity:.62;pointer-events:none}.notice{padding:14px;border-radius:12px;background:var(--blue)}
    @media(max-width:980px){.shell{grid-template-columns:1fr}.rail{padding:18px;display:block}.rail nav{grid-template-columns:repeat(4,minmax(100px,1fr));overflow:auto;margin-top:14px}.local{display:none}.grid{grid-template-columns:1fr}.sticky{position:static}}
    @media(max-width:620px){main{padding:24px 14px 42px}.top{display:block}.status{display:inline-block}.summary-grid{grid-template-columns:1fr}.task{grid-template-columns:1fr}.actions button{flex:1 1 42%}h1{font-size:36px}}
    @media(prefers-reduced-motion:reduce){html{scroll-behavior:auto}}
  </style>
</head>
<body>
  <a class="skip" href="#main">跳到主要内容</a>
  <div class="shell">
    <aside class="rail" aria-label="Development 导航">
      <div class="brand"><span>W</span>ProductWoc</div>
      <small>Local development workflow</small>
      <nav><a href="#dag">任务 DAG</a><a href="#patches">Patch</a><a href="#evidence">Evidence</a><a href="#models">模型</a></nav>
      <div class="local">本地独立模式<br/>Checkpoint 是页面事实源</div>
    </aside>
    <main id="main"><div id="app" aria-live="polite"><p>正在读取本地 Development Checkpoint…</p></div></main>
  </div>
  <script>
  (function(){
    var app=document.getElementById('app');var view=null;var requestedProjectId=new URLSearchParams(window.location.search).get('projectId');var projectId=requestedProjectId||localStorage.getItem('productwoc.developmentProjectId')||'demo-project';
    function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,function(c){return({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'})[c]})}
    function short(v){return esc(String(v||'').slice(0,12))+(v&&v.length>12?'…':'')}
    function key(a){return a+':'+Date.now()+':'+Math.random().toString(36).slice(2)}
    function taskCard(t){var current=view.currentTask&&view.currentTask.taskRunId===t.taskRunId;return '<div class="task '+(current?'current':'')+'"><span><strong>'+esc(t.title)+'</strong><small>'+esc(t.executionTaskId)+' · '+esc(t.status)+'</small><span class="trace">REQ '+esc(t.requirementIds.join(', ')||'—')+' · AC '+esc(t.acceptanceCriterionIds.join(', ')||'—')+' · Design '+esc(t.designItemIds.join(', ')||'—')+'</span></span><span class="pill">Evidence '+t.evidenceCount+'</span></div>'}
    function phaseCard(p){return '<article class="card phase '+esc(p.status)+'"><div class="card-head"><h2>'+esc(p.title)+'</h2><span class="pill">'+esc(p.status)+'</span></div><div class="card-body tasks">'+p.tasks.map(taskCard).join('')+'</div></article>'}
    function patches(){if(!view.patches.length)return '<p class="muted">尚无 Patch Journal。</p>';return view.patches.map(function(p){return '<details><summary>'+esc(p.journalEntryId)+' · '+esc(p.status)+' <span class="pill">'+esc(p.risk)+'</span></summary><div class="detail"><p class="trace">Diff Hash '+esc(p.diffHash)+'</p>'+p.files.map(function(f){return '<div class="file"><strong>'+esc(f.operation)+'</strong><span>'+esc(f.path)+'<small class="trace">'+short(f.beforeHash)+' → '+short(f.afterHash)+'</small></span></div>'}).join('')+'</div></details>'}).join('')}
    function evidence(){if(!view.verification.evidence.length&&!view.verification.logs.length)return '<p class="muted">尚无验证记录。</p>';var es=view.verification.evidence.map(function(e){return '<details><summary>'+esc(e.verificationStepId)+' · '+esc(e.outcome)+'</summary><div class="detail"><p>'+esc(e.summary)+'</p><p class="trace">'+esc(e.type)+' · Workspace '+short(e.workspaceHash)+' · Artifact '+esc(e.artifactId)+'</p></div></details>'}).join('');var logs=view.verification.logs.map(function(l){return '<details><summary>日志 · '+esc(l.verificationStepId)+(l.truncated?' · 已截断':'')+'</summary><div class="detail"><pre>'+esc(l.content)+'</pre></div></details>'}).join('');return es+logs}
    function modelOptions(selected){return view.models.profiles.map(function(p){return '<option value="'+esc(p.profileId)+'" '+(p.profileId===selected?'selected':'')+'>'+esc(p.profileId)+' · '+esc(p.providerType)+' / '+esc(p.model)+(p.status==='available'?' · 可用':' · 需本地配置')+'</option>'}).join('')}
    function models(){var rows=view.models.snapshots.map(function(m){return '<div class="task"><span><strong>'+esc(m.scope)+'</strong><small>'+esc(m.providerType)+' / '+esc(m.model)+'</small></span><span class="pill">'+esc(m.selectionSource)+'</span></div>'}).join('')||'<p class="muted">尚无运行时 Model Snapshot；新 Agent Run 将按当前项目策略选择模型。</p>';var scopes=['development.implementation','development.review','development.repair'];var overrideRows=scopes.map(function(scope){var override=view.models.stageOverrides.find(function(item){return item.scope===scope});var selected=override?override.profileId:view.models.projectDefaultProfileId;return '<div class="file"><label for="model-'+esc(scope)+'"><strong>'+esc(scope)+'</strong><small class="muted">'+(override?'阶段覆盖':'继承项目默认')+'</small></label><select id="model-'+esc(scope)+'" class="model-select" data-model-scope="'+esc(scope)+'" '+(!view.permissions.canConfigureStageModels?'disabled':'')+'>'+modelOptions(selected)+'</select></div>'}).join('');return '<p class="trace">策略 '+esc(view.models.projectDefaultSnapshotId)+' · 配置 r'+view.models.policyRevision+' · 默认 '+esc(view.models.projectDefaultProfileId)+'</p>'+rows+'<details><summary>阶段模型覆盖（所有用户功能一致）</summary><div class="detail">'+overrideRows+'<p class="notice"><strong>切换影响</strong><br/>当前 Agent Run 与既有 Model Snapshot 不会热切换；新配置只作用于之后创建的 Agent Run。若重跑已完成任务，相关 Evidence 与下游结果必须重新复核。</p><button class="secondary" id="save-stage-models" type="button" '+(!view.permissions.canConfigureStageModels?'disabled':'')+'>保存阶段模型覆盖</button></div></details>'}
    function buttons(){var p=view.permissions;return '<div class="actions"><button class="secondary" data-action="pause" '+(!p.canPause?'disabled':'')+'>暂停</button><button class="primary" data-action="resume" '+(!p.canResume?'disabled':'')+'>恢复</button><button class="secondary" data-action="retry" '+(!p.canRetry?'disabled':'')+'>重试</button><button class="secondary" data-action="verify" '+(!p.canVerify?'disabled':'')+'>重新验证</button><button class="secondary" data-action="rollback" '+(!p.canRollback?'disabled':'')+'>回滚当前 Patch</button><button class="primary" data-action="gate" '+(!p.canGate?'disabled':'')+'>确认阶段 Gate</button><button class="danger" data-action="cancel" '+(!p.canCancel?'disabled':'')+'>取消 Run</button></div>'}
    function render(v){view=v;projectId=v.projectId;localStorage.setItem('productwoc.developmentProjectId',projectId);var blockers=v.blockers.map(function(b){return '<div class="blocker '+esc(b.kind)+'"><strong>'+esc(b.title)+'</strong><br/>'+esc(b.detail)+'</div>'}).join('');app.innerHTML='<div class="top"><div><div class="eyebrow">'+esc(v.workspaceId)+' / '+esc(v.projectId)+'</div><h1>让每一个代码变更，都有证据和退路。</h1><p class="lede">当前页面只展示服务端 Checkpoint View Model；刷新或 SSE 重连后会重新查询最终状态。</p></div><span class="status">'+esc(v.statusLabel)+'</span></div><div class="meta"><span class="pill">Checkpoint r'+v.checkpointRevision+'</span><span class="pill">Aggregate r'+v.aggregateRevision+'</span><span class="pill">'+esc(v.safeBoundary)+'</span><span class="pill">Workspace '+short(v.workspaceHash)+'</span></div><div class="grid"><section class="stack"><section id="dag" class="stack">'+v.phases.map(phaseCard).join('')+'</section><article id="patches" class="card"><div class="card-head"><h2>Patch 与 Diff</h2><span class="pill">'+v.patches.length+' 条 Journal</span></div><div class="card-body">'+patches()+'</div></article><article id="evidence" class="card"><div class="card-head"><h2>验证、日志与 Evidence</h2><span class="pill">'+v.verification.evidence.length+' 条 Evidence</span></div><div class="card-body">'+evidence()+'</div></article><article class="card"><div class="card-head"><h2>Repair 历史</h2><span class="pill">'+v.repairs.length+' 次</span></div><div class="card-body">'+(v.repairs.length?'<pre>'+esc(JSON.stringify(v.repairs,null,2))+'</pre>':'<p class="muted">尚无 Repair。</p>')+'</div></article></section><aside class="stack"><article class="card sticky"><div class="card-head"><h2>运行控制</h2></div><div class="card-body">'+blockers+buttons()+'<p class="muted">每个写操作绑定 Idempotency Key、Checkpoint Revision 与 Workspace Hash。</p></div></article><article id="models" class="card"><div class="card-head"><h2>模型与 Provider</h2></div><div class="card-body">'+models()+'</div></article><article class="card"><div class="card-head"><h2>Envelope 校验</h2><span class="pill">'+(v.envelope.valid?'有效':'失效')+'</span></div><div class="card-body trace">'+esc(v.envelope.envelopeId)+'<br/>Hash '+esc(v.envelope.envelopeHash)+'<br/>Plan '+esc(v.envelope.executionPlanVersionId)+'</div></article></aside></div>';Array.from(document.querySelectorAll('[data-action]')).forEach(function(b){b.addEventListener('click',function(){mutate(b.dataset.action)})});var saveModels=document.getElementById('save-stage-models');if(saveModels)saveModels.addEventListener('click',saveStageModels)}
    async function mutate(action){if(!view)return;if((action==='cancel'||action==='rollback')&&!window.confirm(action==='cancel'?'确定取消当前 Development Run？':'确认按当前 Patch Journal 回滚？'))return;document.body.classList.add('busy');try{var body={projectId:projectId,checkpointRevision:view.checkpointRevision,workspaceHash:view.workspaceHash,reason:'用户通过 Development Web 执行 '+action};if(action==='gate'){var phase=view.phases.find(function(p){return p.status==='awaiting_gate'});body.phaseRunId=phase&&phase.phaseRunId;body.userGateId=window.prompt('请输入当前 User Gate ID');if(!body.userGateId)return}var response=await fetch('/api/development/'+action,{method:'POST',headers:{'content-type':'application/json','x-idempotency-key':key(action)},body:JSON.stringify(body)});var data=await response.json();if(!response.ok)throw new Error(data.error);render(data)}catch(e){window.alert(e.message)}finally{document.body.classList.remove('busy')}}
    async function saveStageModels(){if(!view)return;var changed=Array.from(document.querySelectorAll('[data-model-scope]')).map(function(select){return {scope:select.dataset.modelScope,profileId:select.value}}).filter(function(next){var old=view.models.stageOverrides.find(function(item){return item.scope===next.scope});return (old?old.profileId:view.models.projectDefaultProfileId)!==next.profileId});if(!changed.length){window.alert('阶段模型配置没有变化。');return}if(!window.confirm('确认保存模型覆盖？当前 Agent Run 不会热切换；重跑已完成任务时必须重新复核 Evidence 与下游结果。'))return;document.body.classList.add('busy');try{for(var i=0;i<changed.length;i+=1){var next=changed[i];var response=await fetch('/api/development/stage-model',{method:'POST',headers:{'content-type':'application/json','x-idempotency-key':key('stage-model')},body:JSON.stringify({projectId:projectId,checkpointRevision:view.checkpointRevision,workspaceHash:view.workspaceHash,modelPolicyRevision:view.models.policyRevision,scope:next.scope,profileId:next.profileId,impactAcknowledged:true})});var data=await response.json();if(!response.ok)throw new Error(data.error);view=data}render(view)}catch(e){window.alert(e.message)}finally{document.body.classList.remove('busy')}}
    async function startDevelopment(){var response=await fetch('/api/development/start',{method:'POST',headers:{'content-type':'application/json','x-idempotency-key':key('start')},body:JSON.stringify({projectId:projectId})});var data=await response.json();if(!response.ok)throw new Error(data.error);render(data)}
    async function load(){try{var response=await fetch('/api/development?workspaceId=local-workspace&projectId='+encodeURIComponent(projectId));if(response.status===404){app.innerHTML='<div class="notice"><strong>正在从 Planning Envelope 启动 Development…</strong><p>首次启动会创建本地 Checkpoint，不会连接部署或生产服务。</p></div>';await startDevelopment();return}var data=await response.json();if(!response.ok)throw new Error(data.error);render(data)}catch(e){app.innerHTML='<div class="notice"><strong>无法启动 Development Run</strong><p>'+esc(e.message)+'</p><p>确认 Planning 已完成，并同时运行 Planning 与 Development 本地服务；也可以使用 <code>pnpm product-woc develop</code>。</p></div>'}}
    var events=new EventSource('/api/events');events.addEventListener('development',function(event){var next=JSON.parse(event.data);if(next.projectId===projectId)load()});load();
  }());
  </script>
</body>
</html>`;
}
